<UserControl x:Class="GUI.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/GUI;component/Styles/Styles.xaml"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="5"/>
            <RowDefinition/>
            <!--<RowDefinition Height="15"/>
            <RowDefinition/>-->
        </Grid.RowDefinitions>
        <Button x:Name="PlayVsAI"
                Content="Play vs AI"
                Grid.Row="0"/>
        <Button x:Name="PlayVsPlayer"
                Content="Play vs Player 2"
                Grid.Row="2"/>
        <!--<Button x:Name="PlayVsNetwork"
                Content="Play via network"
                Grid.Row="4"/>-->
    </Grid>
</UserControl>
